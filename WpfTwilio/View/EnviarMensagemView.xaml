<UserControl x:Class="WpfTwilio.View.EnviarMensagemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WpfTwilio">
    <Border BorderBrush="Silver" BorderThickness="1" 
            CornerRadius="5" Margin="5" Padding="5">
        <WrapPanel VerticalAlignment="Center">
            <TextBox Text="{Binding CurrentContato.Numero}" MinWidth="50" Name="numero" Margin="10,0,10,0" Width="60"/>
            <TextBox MinWidth="100" Name="mensagem" Margin="10,0,10,0"/>
            <Button Content="Enviar" Margin="10,0,10,0" />
        </WrapPanel>
    </Border>
</UserControl>
